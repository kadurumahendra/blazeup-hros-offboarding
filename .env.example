# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGO_URI=mongodb://localhost:27017/blazeup_hros

# JWT Security
JWT_SECRET=blazeup_hros_super_secret_jwt_key_2026
JWT_EXPIRES_IN=7d

# Client Configuration
CLIENT_URL=http://localhost:5173

# Email / SMTP Configuration (Optional - falls back to safe simulation if missing)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
SMTP_FROM=BlazeUp HROS <no-reply@blazeup.local>
